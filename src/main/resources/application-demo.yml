spring:
  datasource:
    url: jdbc:h2:file:./data/drama;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password: ""
  flyway:
    locations: classpath:db/migration/common
drama:
  provider:
    mode: mock
