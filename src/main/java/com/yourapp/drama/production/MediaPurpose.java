package com.yourapp.drama.production;

/** Business purpose of an image artifact, independent from its persistence table or provider job type. */
public enum MediaPurpose {
    PREVIS,
    KEYFRAME,
    FINAL_REFERENCE
}
