package com.yourapp.drama.production;

/** Whether an image occupies a provider-native boundary slot or remains a semantic reference. */
public enum ProviderFrameMode { NATIVE_BOUNDARY, SEMANTIC_REFERENCE }
