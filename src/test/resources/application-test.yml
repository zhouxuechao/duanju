spring:
  datasource:
    url: jdbc:h2:mem:dramatest;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  flyway:
    locations: classpath:db/migration/common
drama:
  provider:
    mode: mock
  jobs:
    enabled: false
  storage:
    root: ./target/test-media
