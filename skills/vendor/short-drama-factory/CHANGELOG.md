# CHANGELOG

## v3.2.0 （2026-09-17）新增长档（约 3 分钟）篇幅制式

在 v3.1.2 基线上新增第三档篇幅：标准档（90～120 秒）、**长档（165～195 秒，约 3 分钟）**、漫剧档（60～90 秒）；集数档、单元链、付费墙、机检全部按档折算。**无既有行为破坏**：未标"长档"的剧本仍按原档判定。

| # | 变更 | 说明 |
|---|---|---|
| 1 | **长档单集规格** | 165～195 秒 / 正文体量 实拍 650～850（漫剧 520～680）/ 场景 ≤3 / 对白行 ≥8 / 一集两拍（回合A+回合B）+ ≈90 秒中段小钩 |
| 2 | **双回合写法** | 回合B 施压与反驳各升一档；每 30 秒一个新增量；中段小钩只抬价不爆点；场景第 2/3 场必须是升级场；配长档特有 6 条崩坏点自检表（中段泄气/假高潮/注水/场景超载/断章疲软/回合打飘） |
| 3 | **全剧折算** | 内容量守恒 1 长档集 ≈ 2 标准集 → 30/40/50 集三档；四幕占比不变（20/35/25/20）；单元硬帽折半为 10～15 长档集（40 集=3 单元：A 1～13 / B 14～27 / C 28～40） |
| 4 | **付费墙换算** | 一级 5～8 / 4～6 / 7～10；二级 14～16 / 10～12 / 17～20；三级 27～29 / 20～22 / 34～37（分别对应长档 40/30/50 集） |
| 5 | **单集机检分档** | `validate_episode.py` 新增 `--format auto\|standard\|long\|manju`（默认 auto：正文含"长档"→long、含"漫剧"→manju）；E1/E3/E9 阈值按档；**新增 E11：长档缺【本集中段小钩】直接 FAIL** |
| 6 | **全剧机检** | `validate_series.py` 新增长档付费墙区间（30/40/50 集）；`--episodes` 传错档位会报 S6 |
| 7 | **模板与口径同步** | `templates/episode-format.md` 增"档位表 + 长档模板与时间骨架"；`templates/ledger.md` 增"档位"行；beat-sheet / 往返法 / 改编流水线 / 台账 / SKILL / README 全部同步长档口径 |
| 8 | **示例** | 新增 `examples/long-format-3min-demo.md`（约 3 分钟双回合单集，正文体量 711 字，机检 PASSED） |

### 验证

- `validate_episode.py --self-test` PASSED（新增 5 条长档回归：长档体量区间、缺中段小钩报 E11、标准档样本按 long 显式机检报 E1/E11、长档 4 场景报 E3）
- `validate_series.py --self-test` PASSED（新增 2 条长档回归：40 集长档墙落位不报 S6、标准档墙集数按长档核对应报 S6）
- `examples/long-format-3min-demo.md --format long` PASSED（正文体量 711 字）；`examples/ep01-longwang-demo.md` PASSED（364 字，回归无破坏）

## v3.1.2 （2026-09-15）机检漏洞修复

全库体检后按用户裁决修复机检/规则缺陷（无方法论变更，均为口径与脚本修正）。

| # | 问题 | 修复 |
|---|---|---|
| 1 | **E4「前3秒冲突」形同虚设**：`CONFLICT_SIGNALS` 含 `△`，任何以动作行开场的剧本恒过 | 移除 `△`，补入 `断裂/炸/烧/血` 等真实冲突词；新增回归用例（△ 开场废镜头必须被拦） |
| 2 | **E1 体量把字段行算进去**：`【所属阶段】【黄金前3秒钩子】【情绪流变】【本集时长】[场景][人物]` 全计入 | `net_chars()` 改为**只计正文**：对白行 + `△` 动作行 + `【本集断章卡点】`画面；字段行/标记行一律不计 |
| 3 | **E1 与漫剧体量冲突**：beat-sheet 定漫剧 260～400，E1 死锁 350～500 | E1 **按档**：实拍 350～500 / 漫剧（文本含"漫剧"）260～400 |
| 4 | **单句字数三处打架**：`≤15` / `12～18` / `≤25` | 统一：**常规 12～18、爆发金句 8～12、硬上限 ≤25**（SKILL/dialogue-doctor/模板/README 对齐） |
| 5 | **SKILL 模式1 产出仍旧口径**："核心矛盾往返回合表" | 改为"情绪契约单元链 + 每单元一张往返回合表" |
| 6 | **台账模板写入破损**：`...C(__-__)"一个矛盾≤30集` 引号错位 | 修为 `...C(__-__)  # 一个矛盾≤30集` |
| 7 | SKILL/README 标题"七交互模式"实际 8 个 | 改为"交互模式（8 个）" |
| 8 | 钩子库标题"20 大"实际 17 条变体 | 改为"五母型 · 17 变体" |
| 9 | E9 名不副实（"对白回合"实为"对白行数"） | 文案改为"对白行数 ≥4（攻防结构近似）" |
| 10 | S4 把 `声音:`/`旁白:` 当人名报噪音告警 | 白名单前缀过滤（声音/旁白/画外音/内心独白/镜头/字幕/提示/音效） |
| 11 | S1 只认状态精确"埋/养"，写"已埋/未收"漏判 | 宽松匹配：含 埋/养/未收 且非"收"开头即视为待收 |
| 12 | **S4 同一角色同集多行台词重复告警** | 每集内对已报角色去重（`seen` 集合），一个角色只报一次 |
| 13 | **CLI 健壮性**：文件不存在时抛裸 traceback；`--episodes` 非整数/`--script-dir` 缺失无提示 | 统一友好错误提示 + 退出码 2（找不到文件/参数错误）；跳过非 UTF-8 剧本 |

### 验证

- `validate_episode.py --self-test` PASSED（新增 4 条回归：字段行零计、△ 开场拦截、漫剧档、>25 字台词）
- `validate_series.py --self-test` PASSED（新增 3 条回归：S1 宽松状态、S4 噪音名过滤、S4 同名去重）
- `examples/ep01-longwang-demo.md` 正文体量 364 字（阈值内）PASSED
- 集成测试：3 集连写 + 台账 `--script-dir` 全链路通过；S1/S3/S5 叙事一致性检查均正确触发